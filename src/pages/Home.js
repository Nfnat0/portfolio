import React from 'react';
import { Typography, Box } from '@mui/material';

// Home page component to display the home page content
const Home = () => {
  return (
    <Box>
      <Typography variant="h2">Welcome to My Portfolio</Typography>
      <Typography variant="body1">Explore my projects and learn more about me.</Typography>
    </Box>
  );
};

export default Home;
